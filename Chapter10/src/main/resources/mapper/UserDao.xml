<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.code.test.dao.UserDao">
    <select id="getUserCount" resultType="int">
        select count(*) from test_user;
    </select>

    <insert id="addUser" parameterType="com.course.code.test.domain.User">

    </insert>

    <update id="updateUser" parameterType="com.course.code.test.domain.User">

    </update>

    <delete id="deleteUser" parameterType="com.course.code.test.domain.User">

    </delete>

    <select id="getAllUser" resultType="com.course.code.test.domain.User">

    </select>

    <select id="getUserDetail" parameterType="com.course.code.test.domain.UserQueryVo" resultType="com.course.code.test.domain.User">

    </select>

</mapper>